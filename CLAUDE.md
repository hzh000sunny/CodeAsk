# CodeAsk — 项目约定

## UI / 前端设计

涉及任何**页面或组件的设计、重构、视觉打磨**（新建 UI、重写交互形态、调整布局或样式）时，**先触发设计 skill 再动手**，不要凭直觉直接堆样式：

- **`frontend-design`** —— 产出有辨识度、production-grade 的界面，避免通用 AI 审美。偏"实现一个好看的成品"。
- **`ui-ux-pro-max`** —— UI/UX 设计智库（50+ 风格 / 配色 / 字体搭配 / UX 规范 / 组件与图表），也用于 review / 改进 / 优化既有 UI。偏"规划、评审、给方向"。

择一即可：拿不准方向时先用 `ui-ux-pro-max` 规划/评审，再用 `frontend-design` 落地实现；目标明确的单组件直接用 `frontend-design`。

**边界**：纯逻辑 / 数据 / 接口改动（无视觉或布局变化）不强制触发。

## 临时数据 / 测试数据落盘

运行时、测试、调试脚本、集成与 E2E 验证产生的**临时数据或测试数据，禁止写入系统全局 `/tmp`**，必须落在当前用户可写目录下（项目内 `.tmp/`、`CODEASK_DATA_DIR/tmp/`、`~/.codeask/tmp/`，或测试隔离数据目录下的 `tmp/`），并按用途建独立子目录。文档、脚本、命令示例中的路径同样遵循此约定，不要写 `/tmp/...` 或把它当默认方案。

完整规则见 [`docs/rules/temp-directory.md`](docs/rules/temp-directory.md)（含原因：`/tmp` 在容器/受限服务器/企业安全策略下不保证可写、可执行或权限一致）。
