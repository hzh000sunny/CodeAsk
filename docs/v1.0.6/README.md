# CodeAsk 文档 — v1.0.6

| 字段 | 值 |
|---|---|
| 版本 | v1.0.6 |
| 起始日期 | 2026-06-03 |
| 状态 | In progress（会话界面已交付，轨道一 token 重铸未开始） |
| 主题 | 前端视觉重构 —— 设计 token 重铸 + 关键页面重组 |
| 基线版本 | `../v1.0.5/` |
| 目标 | 在不重写组件逻辑、不换框架、不改信息架构的前提下，把 CodeAsk 前端从"功能可用但视觉死板"升级到"有辨识度、有生命感"的成品观感；以会话界面为第一块落地 ink-and-paper 语言，再逐步铺向全站。 |

## 版本定位

v1.0.5 把 Wiki RAG 升级到 OpenViking 后端，主链路与数据归属稳定。v1.0.6 不动后端能力主线，转而做**前端视觉重构**：用户判定会话界面"丑、死板"，展开评估后确认问题既有视觉层也有结构层，但**不需要前端全部重写**——杠杆点在单文件 `frontend/src/styles/globals.css` 与其 token 层，84 个组件中仅 6 个用内联样式，视觉几乎全在 CSS class 层。

本版本采用 `v1.0.6`，语义是：

> 在 v1.0.4/v1.0.5 的产品能力与组件逻辑不变的前提下，把前端观感做成"像新产品"：重铸设计 token（全局焕新）+ 对少数"布局即问题"的页面做结构重组（对话流为第一块），统一到一套 **ink-and-paper 黑白**设计语言。

不改变产品定位、Feature/Wiki/Report/Repo 主数据归属、登录鉴权与权限边界，不换框架、不引入重型组件库。

## 设计总则

**合适的才是最好的。** 每个元素按它自己的角色、密度、和周围的关系单独判断——不拿任何单一颜色或某条配色"分工"当全局答案。需要配色取舍时按语境定、拿不准给候选让用户选。

> 注：早期曾记录过一套硬性配色分工（"炭灰=主色/蓝=唯一 agent 信号/语义实心三件套"等），因反复被当成硬规则机械套用（满屏蓝、满屏墨都被否），已于 2026-06-13 按用户要求全部删除，仅保留上面这一条总则。

## 目录结构

```text
v1.0.6/
├── README.md
└── plans/
    └── frontend-ui-restyle.md   # UI 重构路线（双轨）+ 会话界面已交付记录（§4.1）
                                 # 2026-06-03 从 docs/future/ 提升而来
```

## 与 v1.0.5 的关系

v1.0.5 完成 Wiki RAG 接入 OpenViking、模型配置、同步任务、运行指标、OpenCode 工具权限。v1.0.6 在此基础上：

- **不动后端主线**：会话链路、OpenViking、数据归属、权限不变。
- **新增前端视觉重构主线**（双轨，见 `plans/frontend-ui-restyle.md`）：
  - 轨道一 · 设计 token 重铸（全局，**未开始**）：把 `:root` 扩成完整语义系统（中性灰阶 ramp、elevation、字号/字重/行高、圆角/间距、动效 token），收编散落的硬编码 hex。
  - 轨道二 · 关键页面结构重组（**会话界面已交付**）：去"气泡墙"，改"精炼实录"——用户右对齐小卡、助手说话人沟槽 + 全幅正文、流式 caret、左栏/顶栏/输入区/右栏取证卡全部纳入 ink-and-paper。
- **顺手修复**：流式生成中无法向上滚动的 autoscroll bug。

## v1.0.6 已交付

- 会话界面（对话流 + 左栏列表 + 顶栏 + 输入区 + 右栏取证卡 + 会话弹窗）ink-and-paper 重塑；纯 CSS，不动组件逻辑/DOM，build/lint/tsc/vitest 全绿。
- **调查轨迹收敛为单一来源（右栏「Agent 行动轨迹」）**：曾短暂内联过一份可折叠 `WorkingTimeline`，与右栏 `ActionTracePanel` 是同一份 `ActionTraceEvent[]` 的两个视图，被产品负责人判为"事件重复"。2026-06-03 决策**删内联、保留右栏**，组件 / CSS / 前端邻接 join / 相关测试一并移除（详见 `plans/frontend-ui-restyle.md` §4.1）。

## v1.0.6 待办 / 待拍板

- 轨道一 token 重铸启动时机（把已定型的 ink-and-paper 值收编进 `:root`，再铺向全站，需配视觉回归）。
- 对话流之后纳入轨道二的页面清单与顺序。
- 是否借本次重构一并补齐 light/dark 双主题。
- 是否引入一款标题展示字体（正文/UI 保持系统中性字）。

## v1.0.6 已记录但未修（待数据层决策）

- assistant turn 的 `turn_id` 在 live 与 reload 路径不一致（live = client turn id，reload = agent turn row id，二者无存储链接），导致反馈 badge reload 后丢失。根因需后端数据模型决策，本版未动。（原内联时间线靠前端邻接 join 规避此问题，但内联时间线已于 2026-06-03 删除；右栏 `ActionTracePanel` 自有持久化 trace 通路，不受影响。）
