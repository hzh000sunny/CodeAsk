# 前端 UI 重构路线：设计 token 重铸 + 关键页面重组

> 状态：进行中（轨道二已交付 会话界面 §4.1–4.2、Wiki 页面 §4.3、特性/知识库页 §4.4，并完成图片路径缺陷修复 + 折叠把手全站收口 §4.5；轨道一 token 重铸仍未开始）
> 版本归属：**v1.0.6**（2026-06-03 从 `docs/future/` 提升为本版 UI 主线）
> 主题：在不重写组件逻辑、不换框架的前提下，把 CodeAsk 前端从"功能可用但视觉死板"升级到"有辨识度、有生命感"的成品观感。
> 设计总则：**合适的才是最好的**——按元素角色/语境单独判断，不拿任何单一颜色或配色"分工"当全局答案；拿不准给候选让用户选。详见 [[feedback_visual_taste_fresh]]。（早期那套硬性配色分工已于 2026-06-13 按用户要求删除。）
> 关联：`frontend/src/styles/globals.css`、`frontend/src/components/session/`（对话流为第一块重组对象）、`CLAUDE.md`（UI 设计须先触发 `ui-ux-pro-max` / `frontend-design`）

## 1. 背景与触发

当前会话界面（尤其对话流）被判定为"丑、死板"。展开读取源码后，确认问题既有视觉层、也有结构层，但**不需要"前端全部重写"**。

关键事实（决定了改造成本）：

- 前端样式**集中在单个 `frontend/src/styles/globals.css`（约 7225 行）**，手写 CSS，无真 Tailwind（`tailwind-merge` 仅为 className 合并工具），无 CSS-in-JS。
- **84 个组件中只有 6 个使用内联 `style={{}}`**——视觉几乎全部在 CSS class 层，不在 JSX 逻辑里。
- 已存在 `:root` 变量层（`--bg/--panel/--line/--line-strong/--text/--muted/--soft/--accent/--accent-soft/--green/--shadow` 等），但**偏薄**，大量 hex 仍硬编码散落在那 7000 行里（例如 `.message-bubble` 直接写 `#ffffff/#f3f7ff/#bfd1ff`）。

结论：**换新风格 ≠ 重写前端。** 风格杠杆点是 `globals.css` 与其 token 层；React 组件树与逻辑基本不动。全量重写属于高风险低回报，只有在换框架、上组件库或信息架构本身错误时才划算——而三栏工作台 / Wiki / 设置等结构是健康的。

## 2. 对话流"死板"的具体病因（诊断样本）

以 `frontend/src/components/session/MessageStream.tsx` + `globals.css` 的 `.message-bubble`（约 1414–1481 行）为样本：

1. **所有消息同一个盒子**：user / assistant 都套 760px 白底 1px 边框圆角卡片，堆叠成"方块墙"。成熟 agent 界面从不给助手长答案套盒子，让其作为正文自由排印。
2. **没有说话人身份**：代码有 `messageRoleLabel`（你 / CodeAsk）却未渲染——无头像、无名字、无时间，turn 之间糊在一起。
3. **流式输出是死的**：生成中只有静态"正在生成…"，无首 token 前 shimmer、无 token 流入、无闪烁光标。
4. **agent 工作轨迹被流放右栏**：检索 / 工具调用 / 证据全在 `InvestigationPanel`，与答案割裂；而这恰是代码调查 agent 最"活"的部分。
5. **空状态 / 微交互寡淡**：空态仅一行"等待输入"；操作按钮 hover 才出现；反馈条裸排。

这类"布局本身就是问题"的页面是少数（2–3 个），其余页面靠 token 重铸即可自动焕新。

## 3. 方向：双轨，不大爆炸

### 轨道一 · 设计 token 重铸（一次性，全局生效）

把 `:root` 从十余个变量扩成一套完整语义系统：

- 色彩 ramp（中性灰阶为主体 + 语义色 success/error/warning/info 仅作小面积功能信号）
- 表面层级 / elevation（panel / subtle / raised / overlay）
- 字号阶梯、字重阶梯、行高
- 圆角阶梯、间距节奏（4/8）
- **动效 token**（时长 150–300ms、ease-out 入 / ease-in 出、统一 easing）
- 收编散落的硬编码 hex 进语义色板（中性灰阶 ramp / 状态色等）；具体配色不预先定死，按"合适的才是最好的"在落地时按语境判断（见 [[feedback_visual_taste_fresh]]）。

因 84 个组件吃 class 层、仅 6 个碰内联样式，**重铸 token + 收编硬编码 hex 后，整个产品质感一起焕新，却不碰组件逻辑**。这是"看起来像新产品"性价比最高的一步。

### 轨道二 · 仅对"布局即问题"的少数页面做结构重组

第一块是**对话流**（已与用户对齐方向，见 §4）。其余结构性页面按影响排序，逐块推进，不一次铺满。

### 推进节奏

可分批上、随时停：先重铸 token → 全局焕新 → 再一块块重组关键页面。每批配视觉回归后再合。

## 4. 对话流重组：已对齐的方向

基调与工作轨迹位置已与用户确认：

- **基调：精炼实录。** 沿用现有 token；把"聊天气泡"改为"会自己书写的调查实录 / 案卷"隐喻。
  - 用户 turn → 右对齐紧凑小卡（本就短）。
  - 助手 turn → **去盒子**、全幅正文排印，左侧留"说话人沟槽"（CA 头像 + 名字 + 时间）。
  - 空状态 → "从这里开始 + 建议提问 + CodeAsk 能做什么"。
  - 流式生命感 → 首 token 前 shimmer、token 流入、闪烁 caret；新内容块 `fade+rise` 入场。
- **工作轨迹：内联进对话流。** 在用户问句与最终答案之间嵌入可折叠、实时刷新的竖向连接线时间线（检索 → 工具 → 证据），干完塌缩成一行摘要（"已检索 12 文件 · 调用 3 工具 ▸"）。

参考草图：

```
                          这个鉴权中间件为什么
                          会偶发 401？  14:02      ← 用户：右对齐小卡

╭ CA  CodeAsk · 14:02 ───────────────────────    ← 说话人沟槽，无盒子
│  ◍ 检索代码库 ······· 12 文件                    ← 内联工作时间线
│  │                                              （竖线串起，状态点）
│  ◍ grep "token_expiry" ··· 3 命中
│  ╰ 已折叠 · 展开全部步骤 ▸
│  问题出在 token 的过期判断使用了本地时间▌         ← 正文全幅排印 + caret
│  ⌐ 已解决  部分  没解决       ⧉ 复制  ↻          ← 内联反馈/操作
```

### 4.1 已交付（v1.0.6）：会话界面 ink-and-paper

§4 的"精炼实录"结构已落地。纯 CSS 改动（`globals.css`），不动组件逻辑/DOM，272 测试 + lint + tsc + build 全绿。配色按"合适的才是最好的"逐元素判断（见 [[feedback_visual_taste_fresh]]）——早期记录的硬性配色分工已于 2026-06-13 删除，不再在此罗列。

**已重塑的组件（每个按角色单独设计）：**

- 对话流：空状态、用户右对齐小卡、助手"说话人沟槽 + 全幅正文"、流式 caret、footer 内联操作/反馈条。
- 左栏会话列表（炭灰左条 active）、搜索聚焦、新建按钮。
- 顶栏、输入区（炭灰发送按钮 + 微浮）、右栏容器 + 运行状态卡 + 进度条。
- 右栏 `action-trace` 取证卡：纸感微浮 + hover 抬升 + 运行卡蓝色呼吸。
- 会话弹窗主按钮：`.session-workspace` 范围内的处理。

**调查轨迹：单一来源（2026-06-03 修正）。** §4 草图里曾把工作时间线**内联**进对话流（可折叠 `WorkingTimeline`：步数/工具/证据/失败指标 + live 蓝色扫描线 + `max-height:260px` 滚动贴底），与右栏 `ActionTracePanel`（「Agent 行动轨迹」）并存。但二者吃的是**同一份 `ActionTraceEvent[]`**——内联按本轮过滤、右栏全量——产品负责人判为"事件重复"。决策：**删内联、保留右栏**作为唯一轨迹来源。已移除 `WorkingTimeline.tsx`、其 `.working-timeline*` CSS、`MessageStream` 里的 `insights` prop 与前端邻接 join、以及相关测试；`insights` 仍照常流向右栏 `InvestigationPanel`。

> 复盘：§2 的诊断（"工作轨迹被流放右栏、与答案割裂"）成立，但内联**整条时间线**的代价是与右栏逐事件重复、视觉冗余。结论是工作轨迹归右栏单一承载；若日后想在对话流就近暴露"本轮干了什么"，应是**一行极简摘要**（"已检索 12 文件 · 调用 3 工具 ▸" 跳转右栏），而非复制整条时间线——此项归入轨道二后续待办。

**实现要点 / 已解决的历史问题（详见组件注释）：**

- 顺手修复：流式生成中无法向上滚动的 autoscroll bug（stick-to-bottom 仅在贴近底部时跟随）。
- **未修的历史 bug（已记录、待数据层决策）**：assistant turn 的 `turn_id` live 与 reload 不一致（live=client turn id，reload=agent turn row id，二者无存储链接），导致反馈 badge reload 后丢失。根因需后端数据模型决策，本次未动。（曾用前端邻接 join 在内联时间线侧规避，该时间线现已删除；右栏 trace 自有持久化通路，不受影响。）

### 4.2 已交付（v1.0.6，2026-06-04）：会话界面二次打磨 + 品牌 logo

承 §4.1，对会话三栏继续逐组件打磨，并补上一直缺失的品牌标识。仍为纯 CSS / 组件局部改动，不动框架与信息架构；`tsc` / `eslint` / 相关 vitest / `vite build` 全绿，已推 main。

**右栏 · 行动轨迹（ActionTracePanel）**

- header 用真实"运行中"心跳信号取代通用 Badge：就绪=灰点，运行中=蓝点 + 呼吸 + 涟漪；运行中显示**本轮**动作计数（按 `turnId` 过滤、每轮重置——既不是累计事件数、也不是轮次号或工具次数）。轨迹尾部加 pending beat（墨滴 orb + shimmer「执行中…」），与对话流"正在落笔…"同一套墨滴语汇。
- 空状态从一行"等待输入"升级为"等待第一个问题"——扇形排开的静音 kind chips（Database/FileSearch/Wrench），非骨架屏。
- "Agent 行动轨迹"标题与下方「会话数据」「模型状态」统一（前置 icon + 14px 同字号），三区读作同级。
- 取证卡内容 + 详情弹层精炼（前置状态着色 kind icon、mono 技术片段、重点区 lede + 键值表），弹层再点击可关闭。
- commits：`feedf8e` / `72329fc` / `b03ceca` / `e406310` / `def5f97`。

**右栏 · 会话数据（附件）面板**

- 列表卡改"悬停淡入"：前置文件 chip + 单行 mono 元信息（类型 · 大小 · 短 id），操作簇默认隐藏、hover/focus 显形（`pointer-events` 处理使下层文本仍可选）；空/加载态改静音卡；蓝色卡条退为中性。`0f105c8`。
- 三个操作（重命名 / 编辑用途 / 删除）从浏览器原生 `window.prompt`/`confirm`（显示为页面顶部条）改为 **app 内居中弹窗**（复用既有 `dialog-backdrop`/`confirm-dialog` 样式，Esc/取消、提交中禁用、成功关闭）。新增 `SessionAttachmentDialogs.tsx`。`7f47a51`。
- **放开上传**：删类型白名单 + 大小上限。边界确认：CodeAsk 对附件只负责**原样存进会话工作目录**（`write_bytes`），读取/解析（docx/zip 抽文本）是 opencode/模型侧的事，CodeAsk 不做内容抽取；重命名只改 DB 元数据、删除只删行 + `unlink`，三操作天然类型无关。`3376925`，见 [[feedback_attachment_scope_storage_only]]。

**右栏 · 模型状态面板** — 去蓝色卡条、修正图标（→ `Cpu`）、删失效的蓝渐变进度条规则、静音空/加载态。`0f105c8`。

**会话 header 重构（`SessionHeader`）** — 去掉 `active` 彩色徽章 + `blur`/渐变底，扁平纸面；标题 21px/700 收紧字距 + 单行省略号（hover 出全名）+ 等宽 ID 胶囊；标题下保留一行固定描述副文（取空状态那句"描述你遇到的问题…"，静音灰、自然换行）。空状态 hero 里的同句按用户要求保留（两处都在）。早期尝试过"副文=本轮第一句提问"和"相对更新时间"，均被否（前者与对话流重复、后者无人关注），最终用固定描述句。`2aad3e0`。

**左栏 · 会话列表批量操作** — 新增「全选 / 取消全选」toggle（选中所有可见会话、尊重搜索过滤、与"已选 N 个"计数联动）。`bfe2dc8`。

**对话流 · 切会话即时落底** — `.message-stream` 的 `scroll-behavior: smooth` → `auto`：之前每次 `scrollTop=scrollHeight` 都被浏览器做成动画，切会话时可见地"从上往下滚"；改 `auto` 后瞬间定位底部，并顺带消除流式逐 token 增长时的平滑滚动抖动。`bf54c35`。

**品牌 logo / favicon（此前完全缺失）** — 新增"思考的猫"矢量标记（蓝→紫渐变线稿）：用 `potrace` 描摹原始 PNG → 路径，再以「整幅渐变矩形 + 线稿蒙版」上色（统一一条渐变、不拼色块）。favicon 与导航栏品牌位均采用**裁剪后的纯猫版**——按路径坐标分组、去掉问号/气泡/拖尾小圆点，并裁到猫的边界框填满图标方框，以提升小尺寸辨识度。`index.html` 接 favicon（`rel=icon` + `apple-touch-icon`），`TopBar` 占位「C」方块 → `<img>` logo（`.brand-mark` → `.brand-logo`）。`e64400b`。
  - 资源：`frontend/public/logo.svg`（导航栏 30px）、`frontend/public/favicon.svg`（标签页），当前内容一致（均为纯猫裁剪版）。
  - 遗留：细节线稿在 16px 仍偏糊（细节插画当 favicon 的物理上限）；若要 16px 真正锐利，需更简化的小图标变体（如仅猫头剪影），归后续。

### 4.3 已交付（v1.0.6）：Wiki 页面 ink-and-paper

继会话界面后，Wiki（阅读 + 编辑 + 搜索 + 目录树）整体落到同一套 ink-and-paper 语言。仍为纯 CSS / 组件局部改动，不换框架、不动信息架构；`tsc` / `eslint` / vitest / `vite build` 全绿，已推 main。

- **阅读页排版**：正文落到居中纸张、两侧留灰、发丝边；放宽正文阅读宽度，使收起侧栏后正文能真正变宽（`bc5de9b` / `d385648`）。
- **文档报头 + 右侧大纲**：重做文档 masthead（特性 › 路径面包屑、右对齐操作），新增右侧 TOC 大纲目录（`768c2f9`）。
- **编辑页重做**：与阅读页同构 masthead、源码/预览主次分栏、可拖拽分隔条 + 滚动同步；源码栏接入 **CodeMirror 6** 语法编辑器 + 格式工具条（`d6dc705` / `45554f6`）。
- **搜索框与结果卡片**：脱蓝、先导图标、路径带特性名、摘要截断（`20e7dae`）。
- **目录树折叠把手**：改贴顶扁平发丝把手 + 树收起态加图标（`d24c613`）——即后来 §4.5 全站收口的源头样式。
- **交互/语义修复（顺手）**：带特性进入时在树里高亮并展开该特性，消除「右栏点名、左栏无指向」；历史特性改只读存档、去掉建设/上传入口；未选中时不再误导「建设这个特性」（`b2e7b33` / `6bf6f31` / `c475a41`）。
- **字体**：换 Noto Sans SC 可变字体，修复中文标题/加粗字重丢失（`b706fe7`）。

### 4.4 已交付（v1.0.6）：特性页 · 知识库子页 + 路由持久化

特性工作台的「知识库」子 tab 此前用一套与 Wiki 割裂的简陋树。本轮对齐 Wiki 视觉，并把页面状态接入路由。纯 CSS / 组件改动，相关测试 + tsc + lint + build 全绿，已推 main。

- **知识库子页改用 Wiki 树视觉**：复用 `.wiki-tree-*` 类与角色图标（`resolveNodeTypeIcon` 导出复用）；修掉「树 / 预览五五分导致预览被压窄」——改 CSS Grid `var(--kn-tree)` 默认树窄预览宽，并加可拖拽分隔条（同 Wiki 编辑页拖动条）。预览报头改用 Wiki 同款 chevron 面包屑（特性 › 知识库 › …），按钮文案精简为「进入工作台」（`189cc95` / `c624813` / `81ffde9` / `db96d45` / `e008d6b`）。
- **状态进路由（持久化）**：把「选中的特性 + 子 tab + 知识库内选中节点」从组件本地 `useState` 提升到 `AppRouteState.features`（URL hash 参数 `feature`/`tab`/`node` + `codeask:feature-route` localStorage），跨页面切换 / 浏览器刷新都能恢复。修复两层 bug：点完子 tab 切走再回来 / 刷新会丢、点完某篇 wiki 切走再回来会跳回第一篇（`c971bd9` / `ae10f85`）。

### 4.5 已交付（v1.0.6）：图片路径缺陷修复 + 折叠把手全站收口

- **Wiki 图片路径解析两个 bug**（`0bc7d6b`）：
  1. **中文相对路径渲染不出**：react-markdown 会把 URL 百分号编码（`图片/测试.png` → `%E5%9B%BE%...`），与后端未编码的 `ref.target` 对不上 → 查表落空 → 回退成编码后的相对地址失效。修复：查 `imageSrcMap`/`linkHrefMap`/broken 集合时按原值与 `decodeURI` 解码后两种形态各试一次。
  2. **http(s)/data 外链被当相对路径**：后端 `resolve_reference_path` 把外链当 wiki 相对路径解析、找不到节点 → 误标 broken → 前端渲染「图片无法加载」占位。修复：后端 `parse_markdown_references` 跳过外部地址（`is_external_target`）；前端兜底——外部 URL 永不显示 broken 占位（旧文档无需重新同步即可生效），真正加载失败仍交给 `<img onError>`。
- **二级侧边栏折叠把手全站收口**（`ee5fc83`）：§4.3 把 Wiki 目录树 + 主导航的折叠把手改成了贴顶扁平发丝把手，但会话/特性/设置三个二级栏当时被显式延后（仍用旧「悬浮中点 + 重阴影药丸」）。本次把同一套样式扩展到这三个面板的 `.edge-collapse-button.secondary`（贴顶、零阴影、炭灰 token、静音点缀，rail `:hover`/`:focus-within` 显形、收起态加重）。至此四界面（主导航 + Wiki + 会话/特性/设置二级栏）折叠把手位置、形态、交互完全一致。纯 CSS。

## 5. 边界与约束

- **一致性优先于炫技**：复用既有 token 体系，新风格须与产品其余页面（含 OpenCode 控制台）同一套语言；不从零另开一套调色/字体造成割裂。
- **可访问性**：对比度 ≥ 4.5:1；保留可见 focus ring；图标按钮带 aria-label；`prefers-reduced-motion` 下关闭入场 / 流式动效。
- **视觉回归是硬前置**：token 重铸会触及每一个页面，必须配截图 / Playwright 对比，分批合入。
- **硬编码 hex 收编**：把散落 7000 行里的 hex 提升进 token，机械但必要。
- **设计流程**：按 `CLAUDE.md`，规划 / 评审用 `ui-ux-pro-max`，落地实现用 `frontend-design`。

## 6. 待定项（进入版本前需拍板）

1. ~~**风格基调候选**：是否引入专属展示字体 + 品牌强调色？~~ ✅ 已定：视觉总则为"合适的才是最好的"（不预先定死配色，按语境判断；见 [[feedback_visual_taste_fresh]]）；**展示字体待定**（轨道一可再议是否引入一款标题展示字，正文/UI 保持系统中性字）。
2. **验证方式**：会话界面已作为"第一块样板屏"落地并迭代到位（取代抽象样板，直接在真实页面上拍板）。后续页面沿用此 ink-and-paper 语言。
3. ~~**工作轨迹内联的数据通路**~~ ⮕ **方向已变（2026-06-03）**：工作轨迹不内联，归右栏 `ActionTracePanel` 单一承载（内联整条时间线与右栏逐事件重复，已删）。曾落地的前端邻接 join 随之移除。若日后要在对话流就近提示"本轮干了什么"，仅做一行摘要跳转右栏，不复制时间线——见 §4.1 复盘，归入轨道二后续待办。
4. **重组页面清单与顺序**：对话流之后，哪些页面纳入轨道二、按何顺序（**待拍板**）。含对话流内"一行轨迹摘要"是否补做。
5. **暗色模式**：是否借本次重构一并补齐 light/dark 双主题（token 重铸是最佳时机，**待拍板**）。
6. **轨道一启动**：会话界面已先行落地 ink-and-paper，但 `:root` token 体系尚未重铸、硬编码 hex 仍散落（§3 轨道一）。需决定何时把已定型的 ink-and-paper 值收编进 token、再铺向全站（**待拍板**）。

## 7. 不做什么

- 不换前端框架、不引入重型组件库、不改信息架构（三栏工作台等结构保留）。
- 不一次性大爆炸重写 84 个组件。
- 不在未做视觉回归的情况下把 token 重铸直接铺向全站。
