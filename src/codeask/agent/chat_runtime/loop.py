"""Low-level loop helpers for the chat runtime.

The initial runtime keeps the loop inside `runtime.py`; this module is reserved
as the stable home for extraction once real LLMGateway integration lands.
"""
